[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "ia-workflow"
version = "0.1.0"
description = "Analisador de sentimento utilizando Python"
requires-python = ">=3.10"

[tool.setuptools.packages.find]
where = ["src"]